# PR #4 Final Local Backlog Proof Receipt

Recorded: 2026-07-18T11:06:57Z

## Identity

- Delivery unit: draft PR #4, remote branch `codex/module-graph-hardening`
- Sole mutable worktree: `codex/module-graph-backlog-fix`
- Tested source commit: `308ad586b5825add2b5a639176573adc6c887861`
- Tested source tree: `b7a43134b1dca199fbfb4eb6d812151c6dd4b9aa`
- Base: `main` at `d10b4e9c364f533d621a9cd151bbcd1bb161278c`
- Pre-delivery remote branch and pull head: `99fc7c6e4287f360906efcaa8424899acc204d14`
- Pre-delivery pull merge ref: `8cdb914cab8524f4f31a42330006bd2cd9f23ea4`
- First authenticated SSH delivery checkpoint: branch and pull head `d2e2f3c126d97c2d023377bacbc500d1ed0108b2`; regenerated merge ref `b63a3f4b3544a0a6cd9405b7a227e447031cd01f` with base/head parents `d10b4e9` and `d2e2f3c`
- Delivery checkpoint tree: `8c26d8e5094d48eeba3abd7eecc10849cf1eec8e`, identical to the regenerated merge-ref tree
- Reconciliation documentation following `d2e2f3c` uses the same fast-forward SSH path; live branch/pull equality and merge-ref tree equality are rechecked after each push.
- Source `308ad58` was six commits ahead before delivery and is now contained by the remote PR branch.
- Sole mutable owner: root task in `codex/module-graph-backlog-fix`
- Independent advisory reviewer: `/root/pr4_final_security_review`; read-only and without GitHub or merge authority
- Environment: macOS 26.5.2 build 25F84, arm64, Node 24.13.0, pnpm 11.7.0, Homebrew Rust 1.95.0, declared MSRV Rust 1.85.0, cargo-llvm-cov 0.8.5, Actionlint 1.7.12
- Artifact build id: `sha256:5bce974e13afc903411f9641f4839d38f1851822f05d1de4a947fb4d98ae3fb7`
- Canonical 25-file artifact hash-list SHA-256: `03d9746ec3238f92b4e8d68a255ceae3d51df668593cbaa2517933c000787ef8`
- `ferrite-server.json` SHA-256: `d678916b7adfb7111194e299c35a48c7756504cc278460826b24f208b1e1b0a3`
- `Cargo.lock` SHA-256: `cf5a096c562abdaae60f34b2541e105edd48bc4b546df9090f2892b0e951e0a2`

The documentation commit containing this receipt follows the tested source
commit. It changes no runtime source, tests, manifests, dependencies, or build
configuration. Source and runtime claims below are tied to `308ad58`.

## Source Delta Since The Cached Remote

| Commit | Purpose |
| --- | --- |
| `84759dd` | Snapshot real client build inputs. |
| `e40f0cf` | Close production snapshot audit gaps. |
| `288d774` | Seal asset-resolution snapshots. |
| `2a9e1e3` | Align Windows snapshot paths across Node and Rust. |
| `35700cc` | Prove browser failure cleanup. |
| `308ad58` | Drain timed-out request input before read closure. |

## Reproduced Findings And Fixes

| Finding | Reproduction and fix |
| --- | --- |
| Incomplete client input snapshots | Real CSS, JSON, text, WASM, static assets, and configured roots could affect output without participating in snapshot validation. The build now records real esbuild inputs and revalidates them before publication. |
| Runtime-edge and intrinsic mismatches | Controlled/default SSR and hydration precedence, nullish handling, intrinsic tag normalization, and reserved action props diverged on edge cases. Shared behavior and regressions now cover each case. |
| Cross-route invalidation race | A change outside the currently published route could evade a route-local check. The builder now validates the whole graph before and after manifest construction. |
| Resolver identity gaps | Suffixed symlinks, CSS `url`, `import`, and `composes` references without `./`, outside-project symlink retargets, and exact data URLs were not all represented consistently. Canonical identities and invalidation regressions cover these forms. |
| Windows path mismatch | Rust and Node disagreed on extended drive and UNC paths. Both normalize `\\?\C:\...` to `C:/...` and case-insensitive `\\?\UNC\server\share` to `//server/share`; pure cross-platform tests and Unix retarget integration lock the contract. |
| Child lifecycle gaps | Spawn failure, terminal output drainage, and browser-close rejection could leave incomplete observation or skip server cleanup. Helpers now wait for terminal `close`, retain output, and always stop the owned server. |
| Windows shutdown false positive | The browser helper could accept unrelated Windows termination results. It now accepts exit code 0 or the documented Windows `SIGTERM` result only, with positive and negative tests. |
| Slow-client timeout reset | Both focused and coverage runs reproduced a server `EINVAL` and client reset when the server closed the read half before writing a 408 response. Timeout/parser responses now write fully, half-close the write side, discard unread input under an absolute 25 ms and request-size bound, then close reads. Header and action-body deadline regressions each passed 50 repetitions. |

## Exact-Source Gates

| Gate | Result |
| --- | --- |
| `pnpm lint` | Exit 0: Rustfmt, Clippy with warnings denied, TypeScript, and Node checks passed. |
| `pnpm typecheck` | Exit 0: package checks and the example `ferrite check` for seven routes passed. |
| `pnpm build` | Exit 0: workspace and package builds passed. |
| `pnpm test` | Exit 0: 274 Rust plus 235 Node/browser tests, 509 total, with zero failures, skips, ignored tests, todos, or cancellations. All 10 browser tests executed against real Chrome; real `ferrite build`, dev, render, and production `ferrite serve` flows passed. |
| Rust coverage | `cargo llvm-cov --workspace --all-targets`: exit 0, 91.11% lines, 89.08% functions, and 89.64% regions. The client bundler measured 95.88% lines and the dev server 91.98%. These are measured reports, not enforced thresholds. |
| Runtime coverage | `node --test --experimental-test-coverage test/*.test.mjs`: 164/164 passed, with 80.56% lines, 76.28% branches, and 88.95% functions. `build-client.mjs` measured 93.92% lines, 85.77% branches, and 93.62% functions; `project-path.mjs` measured 100% for all three. |
| Reproducibility | Two consecutive example builds produced the same build id, the same 25 files, and byte-identical contents. The canonical hash-list and server-manifest hashes are recorded above. |
| npm candidates | `pnpm release:verify:npm`: exit 0; protocol, protocol-wasm, runtime, and node tarballs were built and verified. |
| Cargo archives | `pnpm release:verify:cargo`: exit 0 for all 11 crates. Every crate still warns that repository, homepage, or documentation metadata is missing. |
| Dependency and workflow audit | `cargo audit`, `pnpm audit --prod --audit-level high`, and `actionlint .github/workflows/*.yml`: exit 0 with no known Cargo or production pnpm vulnerability. |
| Compatibility | Rust 1.85 entire-workspace/all-target check passed. Stable Linux GNU and Windows GNU entire-workspace/all-target checks passed. Native Windows runtime, junction, and UNC behavior remain unexecuted. |
| Focused timeout stress | Header and action-body deadline tests passed 50 repetitions each, 100 total. |

## Normal, Failure, And Odd Runtime Matrix

| Path | Observed result at `308ad58` |
| --- | --- |
| Normal | Real Chrome loaded artifact-backed HTML, hydrated the page, completed a real server action, and exercised real build, dev, and production serve processes. |
| Failure | Unknown-action and public action-error paths fail closed; spawn failure and browser-close rejection retain terminal evidence and clean owned children; ambiguous request framing is rejected before application handling. |
| Odd | Suffixed/CSS/external/data dependencies invalidate deterministically; Windows path forms agree in Rust and Node; silent, header-trickle, and action-body-trickle clients receive bounded timeout handling. |
| Retry and timeout | Resolver retry validates a fresh whole-build snapshot. Timeout draining uses a fixed 1 KiB buffer, absolute 25 ms cap, request-size cap, and nonrenewing deadline. |
| Cleanup | Tests observed child `close`, browser rejection still stopped the server, no owned Ferrite/Chrome/Playwright/test process remained, and failed build staging stayed bounded and removable. |
| Rollback | The six commits have no persistent state or schema change and are mechanically revertible. Reverting reopens the reproduced snapshot, cleanup, portability, and slow-client defects. Direct artifact-directory activation still lacks a versioned atomic release pointer. |

Production server-action success and failure now run against real
`ferrite serve`. The payload navigator remains fixture-backed because generated production
bundles do not expose the private hydration root handle required by the current
SPA navigator. Adding that production SPA-navigation wiring is a new framework
feature and is not smuggled into this backlog unit.

## Independent Review

The same bounded reviewer followed the delivery unit across its fixes: `FIX` at
`e40f0cf`, `FIX` at `288d774` for Windows path handling, `ACCEPT` at `2a9e1e3`,
`FIX` at `35700cc` for Windows shutdown semantics, and final `ACCEPT` at exact
source `308ad58` and tree `b7a4313`.

The final review found no blocking security, build-integrity, or portability
finding. It independently passed five browser/helper tests, five timeout/header/
body/oversize/drain tests, Rust formatting, and reviewed the 100-repeat deadline
evidence. It verified that response draining is bounded, cannot renew its
deadline, and cannot become a pipelined request because the response declares
`Connection: close`.

Residual nonblocking risk: the two trickle regressions require a 408 status line
but permit a later reset rather than requiring the complete body. The silent
timeout regression separately proves complete body delivery. The reviewer verdict
is technical evidence, not GitHub approval or reserved-action authority.

## Test-Validity Audit

- No constant assertion, tautological assertion, empty test body, ignored Rust
  test, or current-run skip was found.
- Browser tests retain a conditional skip when Chrome is absent, but Chrome was
  present and all 10 browser cases executed in this proof.
- `fake` values are negative CSRF and nonce fixtures. They do not simulate a
  shipped product path.
- The `build-placeholder` marker is a negative assertion requiring placeholder
  output to be absent.
- Compiler, browser, real socket, real child-process, artifact-build, serve, and
  package boundaries were executed; compilation alone is not the evidence basis.

## Backlog Ledger

| Unit | Exact state | Evidence | Disposition and next action |
| --- | --- | --- | --- |
| Draft PR #4 | Base `d10b4e9`; accepted source `308ad58`; first authenticated delivery checkpoint `d2e2f3c`; all later changes are receipt-only reconciliation commits on the same ancestry | All local gates above and independent final `ACCEPT`; the live remote branch and pull head track the current report head through authenticated SSH, with an expected-parent merge ref and identical tree, but the stored `gh` API credential returns HTTP 401, so current checks, reviews, comments, and attached media remain unavailable | `EXTERNALLY_BLOCKED`. Repair or replace the GitHub API credential, then inspect hosted checks and review state on this existing PR. No new PR and no merge/draft-promotion authority. |
| Issue #3 | Cached prior knowledge identifies a broad productionization backlog; current authenticated issue state is unavailable | License, distribution, hosted staging/capacity, session/auth, distributed replay, atomic release pointer, and external observability remain outside PR #4 | `EXTERNALLY_BLOCKED` or future scoped backlog. Do not treat the cached issue state as a fresh GitHub inventory. |
| Historical `codex/nginx-proof-harness` local delta | Preserved in its original registered worktree and overlaps completed HTTP-framing work | Its two helper exports, five stack exports, and every named runtime case exist in source `308ad58`; the accepted source adds 16 runtime cases plus stronger snapshot, interruption, cleanup, HTTP/2, and proxy-rejection proof. Exact dirty-file hashes are recorded below. | `SUPERSEDE_CANDIDATE`. No unique intended behavior needs merging into PR #4. The original remains untouched because close/delete authority is absent. |

## Read-Only Delivery-State Check

At 2026-07-18T11:17:36Z, a credential-free GitHub API probe explicitly
removed `GITHUB_TOKEN`, `GH_TOKEN`, `GIT_ASKPASS`, and `SSH_AUTH_SOCK`. The
repository, open-pull list, PR #4, issue comments, reviews, inline review
comments, and cached-remote-head check-runs endpoints each returned HTTP 404.
This is consistent with a private or otherwise publicly inaccessible repository;
it does not prove that the PR is absent or that its authenticated state is
unchanged.

At 2026-07-18T13:22:57Z, the premise was corrected and the available
authenticated paths were tested directly. The stored GitHub CLI API credential
returned HTTP 401 and the sanitized host profile contained no plaintext token,
but SSH authenticated successfully as `integrate-your-mind`. Live authenticated
Git transport reported branch and PR head `99fc7c6` before delivery. The existing
PR branch was then fast-forwarded through SSH to documentation checkpoint
`d2e2f3c`; the live branch and pull head both converged immediately, and GitHub
regenerated merge ref `b63a3f4` with the expected base/head parents and a tree
identical to the delivered head. This performs no merge or deployment.

Local integration is structurally clean: `main` at `d10b4e9` is an ancestor of
source `308ad58`, and source `308ad58` is an ancestor of documentation checkpoint
`fe7fff6`. A local `git merge-tree --write-tree d10b4e9 fe7fff6` produced tree
`89bba104680dc2a711122d40fbb21c9e7a71e9b9`, identical to the checkpoint tree,
and `git diff --check d10b4e9..fe7fff6` passed. This proves local integration
structure only; it does not replace GitHub mergeability, protection, review, or
check state.

## Local Branch And Worktree Inventory

All 24 non-current local branch tips are ancestors of accepted source `308ad58`.
They contain no committed change absent from PR #4 source. This includes the
clean `codex/http-framing-hardening` worktree at `aabd73b`, the clean old
`codex/module-graph-hardening` worktree at `1ad7c32`, the local `main`, and the
historical production and payload feature branches. The current branch adds only
the two documentation commits following source `308ad58`.

The root `codex/nginx-proof-harness` worktree at `bdbb478` remains intentionally
dirty and was inspected read-only:

| Path | Dirty SHA-256 | Accepted-source SHA-256 | Preservation mapping |
| --- | --- | --- | --- |
| `scripts/verify-nginx-runtime.mjs` | `5e77631e775957581047b2fd33c1699cbab66c76c863a9fe613fd684993f5bd3` | `5a5ed916410f50814ad2ad05b99b19b29dc6d9ea6ba9815211d1f7bc8c9ad6fe` | Every dirty named runtime case exists in `308ad58`; accepted source adds 16 cases plus HTTP/2 and request-target coverage. |
| `scripts/lib/nginx-access-log.mjs` | `d724365ef6092c52b5ae6aaa8514894383211a9c4eb6f8f3fad4ac534cf20537` | `3ba10898a47029a1a55b887fe65c0e6a157382762b452d5e1eae3ea9d2347621` | Both dirty exports exist in `308ad58`; accepted source adds six framing/request-target helpers and stronger log assertions. |
| `scripts/verify-nginx-stack.mjs` | `50957703db1c76ef8a88e9e06174441d595d78b3396a6e0712a60f5e26d084ae` | `8de4c211f467a59fa36ba91640f2fb0440c5971768b340d131c628d660a8699a` | All five dirty exports exist in `308ad58`; accepted source adds clean-source snapshots, interruption handling, input pinning, and stronger cleanup. |

The root worktree also reports `.worktrees/pr4-backlog/` as untracked because it
contains this registered Git worktree; it is not a source delta to stage. Two
prunable `/private/tmp` worktree records point to absent paths and therefore hold
no filesystem copy to inventory or delete. No worktree, branch, dirty file, or
metadata entry was removed.

## Explicitly Not Proven

- Current GitHub draft state, comments, review rollup, branch protection, or
  hosted checks. Git transport is authenticated and current, but the stored
  `gh` API credential returns HTTP 401. Hosted Actions previously ended in
  `startup_failure` before job allocation.
- PR-hosted screenshot/video media through an authenticated supported route.
- Native Windows runtime, junction, UNC, or signal execution.
- Root license and public governance files; registry publication/install; native
  prebuild publication; public CLI installation; or complete Cargo package metadata.
- Hosted staging behind TLS/proxy/process manager/CDN, long-duration capacity,
  multi-instance behavior, live rollback, or external observability.
- Production SPA payload navigation, enforced coverage thresholds, mutation
  testing, session-bound CSRF rotation, distributed replay storage,
  deployment-stable action IDs, or first-class authentication guidance.

## Disposition

The exact source meets the technical and independent-review bar and is contained
by the authenticated remote PR branch. Draft PR #4 is nevertheless
`EXTERNALLY_BLOCKED`, not `READY_FOR_RESERVED_ACTION`, because the GitHub API
credential is invalid and current hosted checks and authenticated PR state are
not proven. The branch fast-forward grants no merge, close, deploy, publish,
release, spend, settings, credential-repair, production, or live-state authority.
