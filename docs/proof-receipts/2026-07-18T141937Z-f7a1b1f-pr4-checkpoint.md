# PR #4 Exact-Source Checkpoint

Recorded: 2026-07-18T14:19:37Z

## Identity

- Delivery unit: draft PR #4, remote branch `codex/module-graph-hardening`
- Sole mutable worktree: `codex/module-graph-backlog-fix`
- Tested source commit: `f7a1b1fc7bce53bfff17f3e66918aa0188353257`
- Tested source tree: `051612a0a2c341fa4dfdd4e5e006688e77c225d3`
- Base: `main` at `d10b4e9c364f533d621a9cd151bbcd1bb161278c`
- Pre-delivery remote branch and pull head: `ccc951905b23b24f5999b27cd6908311d3d952fa`
- Pre-delivery pull merge ref: `c8e77f4ebc311a35496cc675d9856e168919143e`
- Independent advisory reviewer: `/root/pr4_final_security_review`
- Environment: macOS 26.5.2 build 25F84, arm64, Node 24.13.0,
  pnpm 11.7.0, Homebrew Rust 1.95.0, declared MSRV Rust 1.85.0,
  cargo-llvm-cov 0.8.5, Actionlint 1.7.12
- Reproducible artifact build id:
  `sha256:71d2a3bc690bfd761e95d9d45455fd835dc090552f8c4e96bfcc6569697a097f`
- Canonical 25-file artifact hash-list SHA-256:
  `4f907c1b3edf5643045846c8e27ca0892ce89d9a36fd742ef8b94cad6a9571f1`
- `ferrite-server.json` SHA-256:
  `22ec25b501b4289df2bbda69abbff879675ddc7b3bf8f78cf7f3eb19a87edade`

The documentation commit containing this receipt follows the tested source
commit. It changes no runtime source, tests, manifests, dependencies, or build
configuration. Source and runtime claims below are tied to `f7a1b1f`.

## Final Compatibility Finding

The independent reviewer found one TypeScript source-compatibility regression:
the generated `createServerActionErrorResponse` factory had made `code`
mandatory even though legacy callers supplied only `{ message }`.

Commit `f7a1b1f` restores `code?: string` in the canonical Rust generator and its
generated TypeScript mirror. When omitted, the property is absent from the wire
object. A no-emit compile fixture covers legacy and coded calls, and a runtime
regression covers legacy omission while retaining coded and invalid-code paths.
Package publication remains restricted to `dist`; the compatibility fixture
cannot emit or enter the package archive.

The reviewer returned `ACCEPT` for exact scope `ccc9519..f7a1b1f`, with no new
finding. It independently passed the compatibility typecheck, legacy/coded/
invalid-code runtime probes, generated-source check, Rust mirror test, and
`git diff --check`, and confirmed that no output file appeared.

## Exact-Source Gates

| Gate | Result |
| --- | --- |
| `pnpm lint` | Exit 0: Rustfmt, Clippy with warnings denied, TypeScript, and Node checks passed. |
| `pnpm typecheck` | Exit 0: package checks, the new compatibility compile fixture, and example `ferrite check` for seven routes passed. |
| `pnpm build` | Exit 0: workspace and package builds passed. |
| `pnpm test` | Exit 0: 274 Rust and 235 Node/browser tests, 509 executable tests total, with zero failures, skips, ignored tests, todos, or cancellations. The no-emit compatibility regression is additional compile proof. All 10 browser tests executed against real Chrome; real build, dev, render, and production serve flows passed. |
| Rust coverage | Exit 0: 91.12% lines, 89.08% functions, and 89.62% regions. Client bundler measured 95.88% lines and dev server 91.99%. These are reports, not enforced thresholds. |
| Runtime coverage | Exit 0: 164/164 passed, 80.56% lines, 76.28% branches, and 88.95% functions. `build-client.mjs` measured 93.92% lines, 85.77% branches, and 93.62% functions; `project-path.mjs` measured 100%. |
| Reproducibility | Two consecutive builds produced the same build id, the same 25 files, and byte-identical contents. Hashes are recorded above. |
| `pnpm release:verify:npm` | Exit 0: protocol (4 files), protocol-wasm (5), runtime (20), and node (4) candidates passed. |
| `pnpm release:verify:cargo` | Exit 0 for all 11 crates. Crates still warn about missing documentation, homepage, or repository metadata. |
| Security and workflow audit | `cargo audit`, `pnpm audit --prod --audit-level high`, Actionlint, staged/current-delta secret-pattern scans, and `git diff --check` passed. |
| Compatibility | Rust 1.85 whole-workspace/all-target check passed. Stable Linux GNU and Windows GNU whole-workspace/all-target checks passed. Native Windows runtime behavior remains unexecuted. |

## Behavior Matrix

| Path | Observed result at `f7a1b1f` |
| --- | --- |
| Normal | Real Chrome loaded artifact-backed HTML, hydrated the page, completed a server action, and exercised real build, dev, and production serve processes. The coded action-error factory retains its code. |
| Failure | Unknown-action and public action-error paths fail closed; spawn and browser-close failure retain evidence and clean owned children; ambiguous request framing is rejected before application handling. |
| Odd | Legacy action-error callers compile and emit no `code` property. Suffixed/CSS/external/data dependencies invalidate deterministically; Windows path forms agree in Rust and Node. |
| Retry and timeout | Resolver retry validates a fresh whole-build snapshot. Timeout draining has fixed buffer, absolute deadline, and request-size bounds. |
| Cleanup | No owned Ferrite, Chrome, Playwright, Docker, Colima, or test process remained. Failed staging stayed bounded and removable. |
| Rollback | Source commits have no persistent state or schema change and are independently revertible. Reverting `f7a1b1f` reopens the reproduced TypeScript source break. |

## Nginx And Hosted Separation

The exact-source nginx candidate-image stack was not falsely reported green.
The existing Docker Desktop client could not reach a daemon. A clean task-named
Colima profile was attempted only for this gate, but VM image conversion failed
before Docker or project execution with `No space left on device`. The failed
profile was deleted with `colima delete ferrite-pr4-final --data --force`; no
profile, VM process, container, volume, or downloaded image cache remained.

The last fully executed nginx stack proof remains tied to earlier source
`ffd49d0`. Full exact-source Rust HTTP/parser/proxy-adjacent tests pass, but they
do not substitute for the containerized nginx boundary. This is an external
runtime-evidence gap, not a source-test pass.

At the pre-delivery remote head `ccc9519`, GitHub Actions run `29646193229`
completed with `startup_failure`, zero allocated jobs, and no status checks.
Local exact-source proof does not substitute for hosted CI.

## Compatibility And Known Limits

- Wire compatibility is preserved for action error responses: `code` is optional
  and absent for legacy calls.
- Rust source compatibility is not fully preserved. Public pre-alpha structures
  including `ServerActionResponseOutcome::Error` and `ClientBundle` gained fields
  during PR #4. Avoiding those breaks would require a larger API redesign and is
  not hidden inside this final compatibility fix.
- Native Windows runtime, junction, UNC, and signal behavior is unexecuted.
- No registry publication, hosted deployment, attached PR video, production SPA
  payload navigation, external approval, or merge authority is claimed.

## Disposition

Draft PR #4 has zero unresolved review threads, an independent exact-source
technical `ACCEPT`, and complete local lint/typecheck/build/test/coverage/package/
browser evidence. It remains `EXTERNALLY_BLOCKED`, not
`READY_FOR_RESERVED_ACTION`, because exact-source nginx execution and hosted CI
did not run. No merge, close, deploy, release, publish, settings, credential, or
production mutation is authorized by this receipt.
