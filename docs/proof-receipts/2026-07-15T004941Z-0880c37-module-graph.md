# Ferrite module graph final source proof receipt

## Evidence target

- Recorded: `2026-07-15T00:49:41Z`
- Repository: `git@github.com:integrate-your-mind/ferrite.git`
- Pull request: `#4` (`codex/module-graph-hardening` into `main`)
- Source commit: `0880c37fd184b38bddfd60e514a23dc2e4e9c175`
- Source tree: `bb2274933fd6d872d25364af6f6fdd1a11ba3399`
- Remote base commit: `d10b4e9c364f533d621a9cd151bbcd1bb161278c`
- Source remote ref: `origin/codex/module-graph-hardening` matched the source
  commit before this documentation-only receipt was added.
- Source worktree was clean before and after the exact-source gates.

This receipt is a later documentation-only commit. The executable gates below ran
against the exact source commit and tree above. The final `pnpm lint`,
`pnpm typecheck`, `pnpm build`, and `pnpm test` runs were repeated after the last
source commit was created.

## Environment

- macOS `26.5.2` (`25F84`), arm64
- Node.js `v24.13.0`
- pnpm `11.7.0`
- Rust `1.95.0 (59807616e 2026-04-14)`
- Cargo `1.95.0 (f2d3ce0bd 2026-03-21)`
- actionlint `1.7.12`
- No container runtime or proof VM was started for this source-only milestone.

## Exact-source gates

| Command | Exit | Evidence |
| --- | --- | --- |
| `git diff --check` | 0 | The complete PR source diff had no whitespace errors. |
| `pnpm lint` | 0 | Rust formatting, workspace clippy with warnings denied, and package checks passed. |
| `pnpm typecheck` | 0 | TypeScript packages and the seven-route example check passed. |
| `pnpm build` | 0 | The Rust workspace, protocol, WASM, runtime, and native Node package built. |
| `pnpm test` | 0 | 29 workflow tests, 15 client-bundler tests, 112 dev-server tests, 134 runtime tests, 20 native-binding/package tests, four Chromium tests, and the real example build/dev/immutable-serve path passed. |
| `rustup run stable cargo llvm-cov -p ferrite-client-bundler --summary-only` | 0 | 94.68% regions, 96.36% functions, and 96.05% lines. |
| `rustup run stable cargo llvm-cov -p ferrite-dev-server --summary-only` | 0 | 90.93% regions, 91.37% functions, and 91.37% lines. |
| `pnpm release:verify:npm` | 0 | Four real npm tarballs were built, inspected, installed, and smoke-tested. |
| `pnpm release:verify:cargo` | 0 | Every workspace crate packaged; registry metadata warnings are recorded below. |
| `actionlint` | 0 | Workflow syntax passed locally. |
| Source secret-pattern scan | 1 | `rg` found no matching credential or private-key patterns. Exit 1 means no matches. |

The coverage and package gates ran on the final source tree before it was committed;
the tree hash was unchanged when the exact-head lint, typecheck, build, and full test
suite were repeated.

## Behavior proof

| Path | Observed result |
| --- | --- |
| Normal | The shipped TypeScript compiler traversal emits deterministic static ESM, dynamic import, CommonJS `require`, and TypeScript import-equals graph edges. A real esbuild bundle proved that an existing `.js` runtime module wins over a competing `.ts` file, while missing `.jsx`, `.mjs`, and `.cjs` imports fall back to the corresponding TypeScript source extensions. |
| Failure | Unresolved relative imports, cycles, nonportable graph paths, and client references absent from a nonempty graph fail with actionable diagnostics. A value-style import used only as a type reproduced a real esbuild missing-export failure before the emitted-runtime graph fix. |
| Odd | Type-only imports do not create runtime client references. Lexically shadowed `require` calls do not create false edges. Competing unused TypeScript files do not invalidate routes when esbuild selected JavaScript. |
| Invalidation | Successful route builds retain graph nodes and content fingerprints. Editing, deleting, and restoring a transitive dependency advances the dev build id; removing a route prunes its watches. `.cjs`, `.cts`, `.mjs`, and `.mts` app modules are watched. |
| Compatibility | Empty graphs retain legacy acceptance, graph and client-reference order remains deterministic, and the runtime-file-first resolver now agrees with physically observed esbuild behavior. |
| Runtime/browser | Chromium exercised navigation, malformed-payload fallback, generated action forms, and WASM assets. The built example served `/posts/abc` in both dev and immutable artifact modes with build id `sha256:5af61aa666ba1f83c631f20c71dec7d679c252f09e229325a083c3f56110ae36`. |
| Cleanup | Every command reached a terminal result. No server or browser was left running, and generated build outputs were removed after their hashes and results were recorded. |

## Test-validity audit

- Tests invoke the shipped `packages/runtime/bin/build-client.mjs`, the real
  TypeScript compiler API, and the real esbuild dependency. They do not substitute
  a test-only parser.
- The extension-precedence regression inspects an actual generated bundle, so the
  assertion cannot pass from graph metadata alone.
- The implicit type-use regression first reproduced esbuild's missing-export
  failure, then proves the production compiler erases the false runtime edge.
- Dev invalidation tests mutate real files and observe production route build ids;
  they do not manually inject synthetic graph changes.
- Browser tests launch Chromium against built Ferrite assets, and example tests run
  the real CLI build, dev, render, and immutable serve commands.

## Independent current-head review

Exactly one bounded, read-only final reviewer was requested after the source head
stabilized:

- Requested route: `gpt-5.6-sol`, `ultra`, standard service tier
- Ephemeral reviewer thread: `019f6339-9139-7fb0-b254-93e9b25abe49`
- Reviewer turn: `019f6339-99f1-7053-8c93-92bebd2c8e3a`
- Wrapper limit/result: 120 seconds, exit `124`
- Verdict: none returned

The wrapper timed out before emitting an effective-model receipt or review verdict.
No model identity or technical approval is inferred from the requested route, and
no replacement reviewer was launched. The missing verdict is an explicit readiness
gap.

## Hosted and PR evidence

- GitHub reported the source head structurally clean and mergeable, with no
  unresolved review threads.
- Exact-head push run
  [29379926230](https://github.com/integrate-your-mind/ferrite/actions/runs/29379926230)
  ended in `startup_failure` with zero jobs.
- Exact-head pull-request run
  [29379927714](https://github.com/integrate-your-mind/ferrite/actions/runs/29379927714)
  ended in `startup_failure` with zero jobs.
- No hosted status check executed. Local proof is not represented as hosted proof.
- The only completed GitHub review covers an earlier commit and is stale for this
  source head.

## File hashes

```text
3a39eda3d4a6e136cae1e4a4a56811f265d9861b727d98b4ed62af5c2713d968  packages/runtime/bin/build-client.mjs
e16c830c4cda901e820bbc17a6fb8382cce862a70b970232702b8263648e022a  packages/runtime/test/render-page.test.mjs
b1670aabfdb7f3f6acf901fbd7b894fd29764dd52bbada1fbf6a05a2e77abd18  crates/ferrite-client-bundler/src/lib.rs
7e6c4ae9ae66270ea9f915b6e518052040dd431447c1c64a8c91b5052f6ef3ec  crates/ferrite-dev-server/src/lib.rs
e508c2f7b23f8196743ce575a3a97ee0ebb1c005c1f5e9c3eaaaefe572c98775  crates/ferrite-dev-server/Cargo.toml
844a6e39bdceefb7ab50e7204dd4347eb0127ab4ff2da8a9671fad698bd6fe42  Cargo.lock
e17806eb0e7bbb5b9179cc4eb57b7bac0bffeef8b401049424118b0ef084e2b4  README.md
58943dae1def59d33e76ef6604f9f87c3ba61aa263c9bca347ee5c0ba170d621  docs/architecture.md
1e79a061879887cfb8c3f6e6943a22012fe06be3e7f683f9f7d192622f933bd5  dist/npm-packages/npm-package-report.json
```

The npm package report was generated and ignored. Its hash binds the package
inventory used by this proof; the generated output was then removed.

## Compatibility, rollback, and production gaps

- Package exports, conditional package resolution, TypeScript path aliases, and
  non-literal dynamic imports remain outside the compiler-owned relative-module
  graph claim.
- `cargo package` succeeds but warns that crate manifests still omit some
  registry-grade documentation, homepage, or repository metadata.
- The integrated nginx stack was not rebuilt for this source-only module-graph
  change. The full Rust HTTP suites remain green, but no fresh real-proxy result is
  claimed here.
- There is no current-head independent review verdict and no hosted job result.
  PR #4 therefore remains draft and is **not READY**.
- `MERGE_ELIGIBLE`: no. Required current-head review and hosted/repository policy
  evidence are absent.
- `AUTHORIZED_TO_MERGE`: no. No current explicit merge authority was granted.
- No migration, feature flag, secret, production data, or persistent user state is
  involved.
- Source rollback is a revert of `0880c37` followed by `6ed32cb` in reverse
  dependency order. Earlier module-graph commits remain separately revertible.
- No merge, deployment, publication, or release was performed.
