# PR #4 Final Local Review Receipt

Recorded: 2026-07-18T08:10:12Z

## Identity

- Delivery unit: draft PR #4, `codex/module-graph-hardening`
- Tested source commit: `55302c0810f0e1fc2e15d3867451876ccd6203c3`
- Tested source tree: `e5e9f84187e542dce95a4aeaf7818f6c7fd7aa10`
- Base: `main` at `d10b4e9c364f533d621a9cd151bbcd1bb161278c`
- Prior remote PR head: `5dd3f9885b44ca3b7182abe39b20274c43cb2fe2`
- Remote delivery branch after source push: `55302c0810f0e1fc2e15d3867451876ccd6203c3`
- Mutable owner: the root task in the `codex/module-graph-backlog-fix` worktree only
- Independent advisory reviewer: `/root/pr4_final_security_review`; no mutable ownership
- Environment: macOS 26.5.2 build 25F84, arm64, Node 24.13.0, pnpm 11.7.0, Rust 1.95.0, declared MSRV Rust 1.85.0, cargo-llvm-cov 0.8.5
- Artifact manifest: 12,228 bytes, SHA-256 `893b2e3a56328595796d352bafbfa5a018c4a7a3f9401a44278a31df7bb618e4`
- Artifact build id: `sha256:3153c7e353d8962dab8758ce886f7737030bf665c24c14d336de0669cdba1329`
- `Cargo.lock` SHA-256: `cf5a096c562abdaae60f34b2541e105edd48bc4b546df9090f2892b0e951e0a2`

## Reproduced Findings And Fixes

| Finding | Reproduction | Fix and regression |
| --- | --- | --- |
| Incomplete production source snapshot | New A/B tests changed `document.tsx`, `loading.tsx`, or `error.tsx` after the client snapshot but before server-module completion; all three builds incorrectly activated before the fix. | Builder-owned convention roots cross the existing bundler boundary as additive snapshot inputs. The real graph traverses their first-party imports. Three deterministic tests now reject mixed output and prove the prior artifact remains intact. |
| Signal test did not prove accepted-work drain | The old process test signaled an idle server whose renderer exited immediately; it could pass without observing accepted work. A piped stderr implementation also blocked startup when logs filled the unread pipe. | The fixture records accepted work, blocks its real renderer, sends SIGINT or SIGTERM, proves the server stays alive, releases work, observes HTTP 200, then requires exit 0. Stderr is retained in a file and surfaced on failure. |
| Failure cleanup orphaned the renderer | A bounded process probe killed only a direct Node parent and observed its looping Node grandchild still alive. The first revised guard had the same early-unwind risk for Ferrite's renderer. | Every test-owned CLI runs in its own Unix process group. Guard drop kills the owned group and waits for its leader. A negative regression drops the guard before release and proves the grandchild no longer exists. |

The earlier module-graph, cycle, invalidation, slow-client, action-error, deployment, MSRV, and cross-platform reproductions remain recorded in `2026-07-18T071154Z-6f39d66-pr4-backlog.md`; this receipt adds the final snapshot, signal-validity, and cleanup remediation without rewriting that historical evidence.

## Exact-Source Gates

| Gate | Command | Result |
| --- | --- | --- |
| Primary chain | `pnpm lint && pnpm typecheck && pnpm build && pnpm test` | Exit 0. Rustfmt, Clippy with warnings denied, TypeScript/Node checks, workspace/package builds, 269 Rust tests, and 211 Node/browser tests passed: 480 total, zero failures, skips, ignores, todos, or cancellations. The example built seven routes, nine HTML files, and seven server modules, then passed render, dev, and artifact-serve flows. |
| Rust coverage | `RUSTC=$(rustup which --toolchain stable rustc) rustup run stable cargo llvm-cov --workspace --all-targets --summary-only -- --test-threads=1` | Exit 0: 91.00% lines, 88.97% functions, 89.50% regions. Builder 94.85% lines, bundler 95.54%, dev/production server 91.92%, protocol 97.30%. |
| Runtime coverage | `node --test --experimental-test-coverage test/*.test.mjs` in `packages/runtime` | Exit 0: 78.96% lines, 74.32% branches, 88.10% functions. `build-client.mjs` is 89.40% lines; `server.js` is 80.68%. |
| npm candidates | `pnpm release:verify:npm` | Exit 0: four release-shaped tarballs built and verified. |
| Cargo archives | `pnpm release:verify:cargo` | Exit 0: all 11 crates package; each reports the real missing repository/homepage/documentation metadata gap. |
| Dependency/workflow audit | `cargo audit`; `pnpm audit --prod --audit-level high`; `actionlint .github/workflows/*.yml` | Exit 0: no known Cargo or production pnpm vulnerabilities; workflows lint cleanly. |
| Declared MSRV | explicit rustup Rust 1.85 `cargo check --workspace --all-targets` | Exit 0. The `RUSTC` path was pinned explicitly so Homebrew Rust could not invalidate the claim. |
| Cross-targets | explicit rustup stable `cargo check -p ferrite-cli` for `x86_64-pc-windows-gnu` and `x86_64-unknown-linux-gnu` | Exit 0. Native Windows runtime behavior remains unexecuted. |
| Test validity | marker scans plus command output | No vacuous true assertions, ignored Rust tests, conditional skips on this host, or production mock/fake/simulated markers were found. Empty binary/doc-test harnesses are not counted in the 269 Rust tests. |
| Secret patterns | tracked names, current diff, and full patch history | No high-confidence key/token/private-key pattern or tracked credential-like file was found. Gitleaks and TruffleHog were not installed, so this is not a dedicated scanner claim. |

Transient owned log hashes:

- Primary chain: `70717a35f0cb35791d204b8ceeab257baa73056bd5a504d1550b8d437ce67cef`
- Rust coverage: `31f8dbbdf6872b7d4b7d7ebe32fa6528691ec35ac44948f1cbc517ffaede1468`
- Node coverage: `7ec493a5d5fa657d178302dcfa0ffa7cc22cb5d1b3f29a17ebe22ee24285be85`
- Package/dependency/workflow audit: `c9a23b7a71f570fe24b364a44478569c6402994a2cd5fd5faecb70e52e4333fc`
- MSRV and cross-target checks: `2cf9850589dda4263d2bbfae24cff6800097168b9f2d37dba53b6274c6f6cda8`
- Direct runtime response matrix: `150cf0a5e3f6352c56dfb221966f5c3e96e8e2462b48c5a24ed6cfe16cfdbdd5`
- Direct runtime stderr/access log: `3e9ceab66b487297250f18bc754490dd0d1701eaa97e7320140508b937d9dcee`

## Runtime Matrix

| Path | Observed result |
| --- | --- |
| Normal | The built artifact server returned HTTP 200 for `/posts/abc`; the body contained `Post abc`. |
| Failure | A raw duplicate-`Content-Length` action request returned HTTP 400 before application handling. |
| Odd | A raw absolute-form target returned HTTP 400. An incomplete slow header with a 200 ms absolute read budget returned HTTP 408. |
| Signal | Real SIGINT and SIGTERM tests held accepted renderer work, observed no early server exit, released the work, received HTTP 200, and required exit 0. Direct runtime SIGTERM also exited 0. |
| Browser | Four sequential Chromium cases passed for navigation/prefetch/popstate, malformed payload fallback, generated action bootstrap paths, and WASM assets. The browser action server remains a fixture rather than real `ferrite serve`. |
| Cleanup | The negative process-group regression removed an unreleased Node grandchild. No owned server, renderer, cleanup fixture, or browser remained after terminal commands. |
| Rollback | Staged builds preserve or restore the prior artifact on activation failure. Reverting this PR is source-only; it has no migration, feature flag, external message, or live-state mutation. Direct-directory activation still lacks a versioned atomic release pointer. |

## Independent Review

The same independent reviewer that found the snapshot and cleanup gaps re-read the exact five-file diff at source commit `55302c0` and returned `ACCEPT_FOR_LOCAL_READINESS`. It reported no Critical, High, Medium, or Low actionable finding. The verdict covers snapshot roots and transitive imports, A/B preservation, accepted-work SIGINT/SIGTERM behavior, process-group cleanup, additive bundler API compatibility, Unix gating, MSRV/cross-target evidence, and rollback scope.

This is a technical local verdict only. It is not GitHub approval and grants no merge, close, deploy, publish, release, settings, credential, spending, production, or live-state authority.

## External And Productization Gaps

- The GitHub GraphQL request returned HTTP 401, so current draft state, comments, reviews, and check rollup could not be authenticated or refreshed. Existing Git refs prove the branch and pull ref independently.
- Hosted Actions previously ended in `startup_failure` before job allocation and supplied no status check. Local parity does not establish hosted execution.
- No PR screenshot or video is attached through a supported authenticated route.
- The repository has no root license, `SECURITY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, or changelog. All four npm source manifests remain private and `UNLICENSED`; Cargo declares MIT without a root license or repository metadata.
- Registry publication, native prebuild publication, a public CLI install, hosted TLS/proxy/process-manager/CDN/rollback proof, real browser-to-`ferrite serve` action/payload proof, session-bound CSRF, distributed replay, deployment-stable action IDs, and external tracing remain unproven.

## Disposition

PR #4 is `EXTERNALLY_BLOCKED` as a delivery unit. Its exact source commit is locally proven and independently accepted, but hosted checks, authenticated PR state, and attached PR media are not established. `READY_FOR_RESERVED_ACTION` is therefore not claimed for the GitHub delivery unit. The PR stays draft, and no reserved action is authorized by this receipt.
