# PR #4 Backlog Proof Receipt

Recorded: 2026-07-18T07:11:54Z

## Identity

- Delivery unit: draft PR #4, `codex/module-graph-hardening`
- Tested source commit: `6f39d6627cb939c6948a9cf05bb72ede9d11b5e5`
- Tested source tree: `ece931ca7f55d8348476787883eb171a054c2e34`
- Base: `main` at `d10b4e9c364f533d621a9cd151bbcd1bb161278c`
- Mutable owner: the `codex/module-graph-backlog-fix` worktree only
- Remote branch and PR ref at capture: `1ad7c32ec7d4ea4bf13d1d29a21fdb3b426cc359`
- Environment: macOS 26.5.2 build 25F84, arm64, Node 24.13.0, pnpm 11.7.0, Homebrew Rust 1.95.0, rustup Rust 1.85.0, cargo-llvm-cov 0.8.5
- Artifact manifest: 10,730 bytes, SHA-256 `0967ac78d935dda93b0fb7770411f81f356b6e64a6543f09d5ce1514f78a1345`
- Artifact build id observed by the direct runtime: `sha256:5d4a2781ff97ad5cf21c00942768e78a24ca0b2170f6e0d0ba6f75e090375d3d`

The coverage commands ran immediately before the source commit while the index/worktree tree was `ece931ca7f55d8348476787883eb171a054c2e34`; the commit records that same tree. All remaining commands below ran with `HEAD` at the tested source commit and a clean tracked worktree.

## Reproduced Findings

| Finding | Pre-fix observation | Fix and regression |
| --- | --- | --- |
| Node-to-Rust graph handoff race | Node could validate source A, emit bundle A, and Rust could fingerprint later source B | Digest-bearing source/resolution snapshot crosses the process boundary; Rust validates before acceptance, retries one stale handoff, and production revalidates after render. A deterministic A/B build rejects mixed output. |
| Missing and cross-root compiler inputs | Creating a root `tsconfig.json`, changing an inherited external config, or changing a symlink target did not invalidate dev output | Missing, local, inherited, external, and symlinked configs are snapshot inputs; appearance/external/symlink regressions pass. |
| Renewable slow-client deadline | Header/body trickle could renew per-read timeouts | One absolute request deadline covers all reads; real header and action-body trickle tests return `408` within the original budget. |
| Unbounded replay state | In-process action nonces had no hard capacity | The store is capped at 4,096, expires/evicts in issuance order, consumes once, and does not allocate on unmatched routes. |
| Internal action errors exposed as protocol errors | Arbitrary action exceptions could be serialized as application-visible error text | Only `FerriteActionError` carries a validated public code/message; unexpected errors reach the generic production `500` boundary and detailed stderr. |
| Deployment signal forwarding | The container shell could absorb signals and the CLI had no owned drain adapter | Docker uses `exec`; CLI SIGINT/SIGTERM sets the shared shutdown flag, stops admission, joins accepted work, and exits cleanly in a real process test. |
| Windows compile regression | The first Windows cross-check failed because Signal Hook's iterator is Unix-only | The CLI uses Signal Hook's cross-platform flag API; Windows and Linux target checks pass. |
| Declared MSRV mismatch | Rust 1.85 rejected a pre-existing let-chain in the production worker loop | Equivalent nested control flow compiles across the complete workspace on Rust 1.85. |

## Exact-Source Gates

| Gate | Command | Result |
| --- | --- | --- |
| Lint | `pnpm lint` | Exit 0: rustfmt, workspace/all-target Clippy with warnings denied, TypeScript package checks, Node syntax checks. |
| Typecheck | `pnpm typecheck` | Exit 0: all TypeScript packages and the seven-route example check. |
| Build | `pnpm build` | Exit 0: Rust workspace, protocol, WASM, runtime, and native binding. |
| Full tests | `pnpm test` | Exit 0: 264 Rust unit/integration tests plus 210 Node/browser tests; zero failures, skips, ignores, todos, or cancellations. |
| Rust coverage | `rustup run stable cargo llvm-cov --workspace --all-targets --summary-only -- --test-threads=1` | Exit 0: 91.11% lines, 89.08% functions, 89.63% regions. Client bundler 96.29% lines; dev/production server 92.15% lines. |
| Runtime coverage | `node --test --experimental-test-coverage test/*.test.mjs` in `packages/runtime` | Exit 0: 78.95% lines, 74.29% branches, 88.07% functions. `build-client.mjs` 89.44% lines; `server.js` 80.68% lines. |
| npm candidates | `pnpm release:verify:npm` | Exit 0: four real tarballs built, inspected, installed together outside the workspace, imported, checked, built, and artifact-served. |
| Cargo archives | `pnpm release:verify:cargo` | Exit 0: all 11 workspace crates package. Warnings correctly expose missing repository/homepage/documentation metadata. |
| Dependency audit | `cargo audit`; `pnpm audit --prod --audit-level high` | Exit 0: no known vulnerabilities in 68 locked Cargo dependencies or production pnpm dependencies. |
| Workflow audit | `actionlint .github/workflows/*.yml` | Exit 0. |
| Declared MSRV | `RUSTC=<rustup-1.85-rustc> rustup run 1.85.0 cargo check --workspace --all-targets` | Exit 0. |
| Linux compatibility | `RUSTC=<rustup-stable-rustc> rustup run stable cargo check -p ferrite-cli --target x86_64-unknown-linux-gnu` | Exit 0. |
| Windows compatibility | `RUSTC=<rustup-stable-rustc> rustup run stable cargo check -p ferrite-cli --target x86_64-pc-windows-gnu` | Exit 0. Native runtime behavior was not executed. |
| Secret scan | tracked tree, full Git patch history, and current-delta patterns for common private keys/provider tokens | No matches. No dedicated Gitleaks/TruffleHog binary was installed. |

Transient exact-source log hashes were recorded before owned scratch cleanup:

- Quality/build/test chain: `ea628f2b3adc9c4099f34f4763c0bea962d0aba95ed23d9c7d395c0957fdbbd9`
- Release/security/compatibility chain: `1c4bc2a4ca078d5a5b4d632c46183367af9e250ed607cfdb8146621f661421bb`
- Rust coverage: `854cb3101328bd966a0ca17effa0ccf897b17d524fbf1bdcef87f3795a25de89`
- Node coverage: `3c715889a791e0c9b5caf9e935b02e28e696b5097557ebe4fe2510979f1168b7`

## Runtime Matrix

| Path | Observed result |
| --- | --- |
| Normal | Built `target/debug/ferrite serve` returned `200` for `/posts/abc`; output contained `Post abc` and a real client-reference marker. |
| Failure | A malformed real action POST returned `400`; unexpected action exceptions separately return a generic real-socket `500` without the internal message. |
| Odd | A raw absolute-form request target returned `400` with the origin-form rejection boundary. Header and action-body trickle regressions returned `408` without renewing the deadline. |
| Browser | Four sequential Chromium tests passed for prefetch/stream/popstate, malformed clicked payload fallback, generated action bootstrap paths, and WASM assets. The browser action server remains a fixture, not real `ferrite serve`. |
| Cleanup | The real server received SIGTERM, stopped admission, drained, and exited 0. No test/browser/server process remained after its owning command. |
| Rollback | Builds remain staged and preserve/restore the previous artifact on activation failure. PR rollback is a normal commit revert; there are no migrations, feature flags, external messages, or live-state changes. |

## Test Validity

- The full command executed each intended pnpm project. An earlier exploratory filter invocation that printed `No projects matched the filters` was rejected as evidence; the quoted protocol command then ran six real tests and its generated-source check.
- Cargo's `0 tests` summaries belong only to empty binary and doc-test harnesses and are not counted in the 264 Rust tests above.
- The only conditional skip is the resolver symlink-race test on Windows. It executed on this macOS run; Windows filesystem casing, junction, UNC, cross-drive, and native signal behavior remain unproven.
- Focused regressions assert response bytes, build ids, fingerprints, graph contents, side-effect absence, process exit, and real socket behavior; compilation alone is not used as feature proof.

## Remote And Release Separation

- The independent reviewer returned `FIX_NOW` at `1c019f9` for the snapshot handoff and config-watch gaps. A final review of the receipt head is still required.
- At capture, Git refs prove the remote branch and PR head remain `1ad7c32`; the source commit has not yet been pushed.
- The GitHub CLI is currently unauthenticated and anonymous API requests are rate-limited. No credential state was inspected or changed, so current comments, draft state, and checks could not be refreshed.
- Hosted Actions previously ended in `startup_failure` before allocating a job and supplied no status check. Local parity is evidence, not hosted execution.
- There is no root license, security/contribution/code-of-conduct/changelog policy, coherent npm/Cargo license metadata, real repository metadata, published registry artifact, hosted staging deployment, attached PR video, or distinct external approval.
- Disposition at receipt creation: `FIX_NOW`, pending receipt commit, existing-branch push, final independent review, and remote-state refresh. This receipt does not authorize merge, close, deploy, publish, release, settings, credentials, spending, or live-state mutation.
