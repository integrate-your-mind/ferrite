import DocsNav from "../../components/DocsNav";
import { siteUrl } from "../../metadata";
export const metadata = { title: "Getting started · Ferrite", description: "Source-build Ferrite onboarding.", alternates: { canonical: siteUrl("/docs/getting-started") } };
export default function GettingStarted() { return <div className="wrap article"><p className="kicker">Docs / Getting started</p><h1>Start from a source checkout.</h1><p>The CLI and runtime are source-backed today. Use Node 22+, Rust 1.95, and the repository package manager.</p><DocsNav/><h2>Check, build, serve</h2><pre><code>{"pnpm install --frozen-lockfile\\ncargo build --workspace\\npnpm --dir website install --ignore-workspace --frozen-lockfile\\npnpm --dir website check\\npnpm --dir website build\\npnpm --dir website start"}</code></pre><div className="notice warning"><b>Not a registry install.</b> The portable npm prerelease does not include the public CLI or an application starter. Do not use a raw generated skeleton as onboarding.</div><h2>What to inspect</h2><ul><li><code>app/layout.tsx</code> owns shared UI and metadata.</li><li><code>app/document.tsx</code> owns the HTML document and global style injection.</li><li><code>\"use client\"</code> components become browser islands with serialized props.</li></ul></div>; }
