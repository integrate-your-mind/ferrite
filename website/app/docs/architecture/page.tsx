import DocsNav from "../../components/DocsNav";
import { siteUrl } from "../../metadata";
export const metadata = { title: "Architecture · Ferrite", description: "Ferrite source-to-artifact architecture.", alternates: { canonical: siteUrl("/docs/architecture") } };
export default function Architecture() { return <div className="wrap article"><p className="kicker">Docs / Architecture</p><h1>One graph, named boundaries.</h1><p>Ferrite keeps route discovery, dependency edges, page rendering, browser bundles, and artifact serving explicit. Rust is the control plane; TypeScript remains the authoring surface.</p><DocsNav/><div className="dark-panel"><pre>{"TSX app directory\\n       │\\n       ▼\\nNode page runner ─── client import guard\\n       │\\n       ▼\\nRust route graph + SSR + protocol validation\\n       │\\n       ▼\\n.ferrite/build (manifest, server output, immutable assets)\\n       │\\n       ▼\\nArtifact-only Ferrite serve"}</pre></div><h2>Client island boundary</h2><p>A component beginning with <code>\"use client\"</code> is hydrated in the browser. Props must be serializable; browser APIs belong in effects, not server render.</p><h2>Evidence boundary</h2><p>Source checks and local captures prove different things. This site labels the pinned Docs Workbench screenshots separately from current-head build, hosted, and production claims.</p></div>; }
