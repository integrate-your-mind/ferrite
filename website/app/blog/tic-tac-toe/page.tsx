import ExamplesIsland from "../../examples/ExamplesIsland";
import { siteUrl } from "../../metadata";
export const metadata = { title: "Build Tic-Tac-Toe in Ferrite", description: "A practical Ferrite client-island walkthrough using typed state and immutable updates.", alternates: { canonical: siteUrl("/blog/tic-tac-toe") } };
export default function TicTacToePost() { return <div className="wrap article"><p className="kicker">Blog / Interactive walkthrough</p><h1>Build Tic-Tac-Toe in Ferrite.</h1><p>Ferrite keeps the familiar state model readable while changing the runtime contract underneath: import hooks from <code>@ferrite/runtime</code>, keep browser interaction in a client island, and pass serializable props across the server boundary.</p><h2>1. Model the board</h2><pre><code>{"type SquareValue = \\\"X\\\" | \\\"O\\\" | null;\\ntype BoardState = SquareValue[];\\nconst [state, setState] = useState<BoardState>(Array(9).fill(null));"}</code></pre><h2>2. Update immutably</h2><pre><code>{"const nextState = [...state];\\nnextState[index] = next;\\nsetState(nextState);"}</code></pre><h2>3. Try the composed island</h2><p>The same site component powers the live board below and the broader examples page. This is a local runtime demonstration, not a hosted Ferrite service claim.</p><ExamplesIsland/><h2>4. Keep the proof honest</h2><p>Check the source-built app, then run the normal, failure, and odd paths yourself. A green local render does not prove CI, distribution, or production readiness.</p></div>; }
