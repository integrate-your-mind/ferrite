export const metadata = { title: "Ferrite 3-D Tic-Tac-Toe with Three.js", description: "A Ferrite walkthrough for isolating a browser-only Three.js integration.", alternates: { canonical: siteUrl("/blog/tic-tac-toe-3d") } };
export default function BlogThreeDTicTacToePost() { return <div className="wrap article"><p className="kicker">Blog / Experimental integration</p><h1>Three.js belongs behind a clear island boundary.</h1><p>The existing Tic-Tac-Toe example demonstrates where a browser-only renderer would mount. This article documents the boundary without pretending this website has a verified Three.js runtime integration.</p><div className="notice warning"><b>Experimental status.</b> Three.js is not bundled into this site. Add it only after a source-build check, client import guard, and browser proof.</div><h2>Import from a client module</h2><pre><code>{"\\\"use client\\\";\\nimport * as THREE from \\\"three\\\";\\nimport { useEffect, useRef } from \\\"@ferrite/runtime\\\";"}</code></pre><h2>Mount and clean up</h2><pre><code>{"const mountRef = useRef<HTMLDivElement>(null);\\nuseEffect(() => {\\n  if (!mountRef.current) return;\\n  const renderer = new THREE.WebGLRenderer();\\n  mountRef.current.appendChild(renderer.domElement);\\n  return () => renderer.dispose();\\n}, []);"}</code></pre><p>Read the source example at <code>examples/basic/app/tic-tac-toe-3d/page.tsx</code>, then validate the exact dependency and browser path separately.</p></div>; }
import { siteUrl } from "../../metadata";
