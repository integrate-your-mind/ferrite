export default function RenderingLifecycle() { return <div className="wrap article"><p className="kicker">Compare / Series</p><h1>Rendering and lifecycle.</h1><p>Ferrite renders a serializable tree on the server and hydrates explicit <code>"use client"</code> islands. Browser-only work belongs in <code>useEffect</code> or refs; rendering must not depend on window or document.</p><table className="table"><caption>Rendering and lifecycle contract comparison</caption><tbody><tr><td>Server render</td><td>Ferrite validates a render packet; React/Next has its own server component contracts.</td></tr><tr><td>Hydration</td><td>Ferrite hydrates registered client references; verify library compatibility per route.</td></tr><tr><td>Cleanup</td><td>Effects must return cleanup for timers, listeners, and renderers.</td></tr></tbody></table></div>; }
export const metadata = { title: "Rendering and lifecycle · Ferrite comparison", alternates: { canonical: siteUrl("/compare/rendering-lifecycle") } };
import { siteUrl } from "../../../metadata";
