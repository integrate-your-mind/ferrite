import { siteUrl } from "../metadata";
export const metadata = { title: "Ferrite vs React and Next", description: "A practical, caveated multi-page comparison series.", alternates: { canonical: siteUrl("/compare") } };
const topics = [["State and events", "/compare/state-events"], ["Rendering and lifecycle", "/compare/rendering-lifecycle"], ["Routing, data, build", "/compare/routing-data-build"], ["Migration matrix", "/compare/migration-matrix"], ["Incompatibilities", "/compare/incompatibilities"], ["Codemod boundary", "/compare/codemod-boundary"], ["When to stay on React", "/compare/when-to-stay"]] as const;
export default function CompareIndex() { return <div className="wrap article"><p className="kicker">Compare / Decision guide</p><h1>Compare contracts, not slogans.</h1><p>Ferrite is inspired by React-like authoring and Next-like routing, but it is not a drop-in replacement. Walk through the boundaries that change real application work.</p><div className="grid-3">{topics.map(([label, href]) => <article key={href}><h3><a href={href}>{label}</a></h3><p>Practical notes, status labels, and migration questions.</p></article>)}</div><div className="notice warning"><b>Decision rule.</b> Keep React when ecosystem breadth, established team knowledge, or compatibility is more valuable than experimenting with Ferrite's explicit source-to-artifact contract.</div></div>; }
