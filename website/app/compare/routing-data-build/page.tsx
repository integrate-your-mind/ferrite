export default function RoutingDataBuild() { return <div className="wrap article"><p className="kicker">Compare / Series</p><h1>Routing, data, build, tooling.</h1><p>Ferrite's app directory is source-built by the Rust CLI. Filesystem routes and layouts become an artifact manifest; the site adapter does not rebuild source.</p><table className="table"><caption>Routing, data, build, and tooling comparison</caption><thead><tr><th>Concern</th><th>Ferrite</th><th>React / Next comparison</th></tr></thead><tbody><tr><td>Routes</td><td>App directory + artifact route graph</td><td>React needs a router; Next owns its own app/pages contracts</td></tr><tr><td>Data</td><td>Explicit server render or same-origin fetch in islands</td><td>Framework loaders and cache semantics differ</td></tr><tr><td>Build</td><td>Rust CLI + Node page/client runners</td><td>React bundler choice; Next compiler/runtime</td></tr></tbody></table></div>; }
export const metadata = { title: "Routing, data, and build · Ferrite comparison", alternates: { canonical: siteUrl("/compare/routing-data-build") } };
import { siteUrl } from "../../../metadata";
