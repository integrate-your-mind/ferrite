export default function Incompatibilities() { return <div className="wrap article"><p className="kicker">Compare / Series</p><h1>Known incompatibilities.</h1><ul><li>Do not import <code>react</code> hooks in a Ferrite client island; use <code>@ferrite/runtime</code>.</li><li>Props crossing SSR to a client reference must be JSON-serializable.</li><li>React Server Components and Next server actions are not automatically understood.</li><li>Browser-only libraries need an explicit island and cleanup.</li><li>Deployment adapters cannot make unsupported runtime behavior supported.</li></ul><div className="notice error"><b>Fail closed.</b> If the route graph, client import guard, or artifact manifest rejects a pattern, change the design rather than weakening the check.</div></div>; }
export const metadata = { title: "Incompatibilities · Ferrite comparison", alternates: { canonical: siteUrl("/compare/incompatibilities") } };
import { siteUrl } from "../../metadata";
