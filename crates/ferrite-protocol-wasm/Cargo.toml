[package]
name = "ferrite-protocol-wasm"
description = "WebAssembly validation bindings for Ferrite server payload protocols."
version.workspace = true
edition.workspace = true
homepage.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
ferrite-protocol = { path = "../ferrite-protocol", version = "=0.1.0" }
ferrite-ssr = { path = "../ferrite-ssr", version = "=0.1.0" }
serde_json = { workspace = true }
